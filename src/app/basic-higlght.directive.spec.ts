import { BasicHiglghtDirective } from './basic-higlght.directive';

describe('BasicHiglghtDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicHiglghtDirective();
    expect(directive).toBeTruthy();
  });
});
