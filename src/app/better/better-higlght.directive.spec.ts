import { BetterHiglghtDirective } from './better-higlght.directive';

describe('BetterHiglghtDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHiglghtDirective();
    expect(directive).toBeTruthy();
  });
});
